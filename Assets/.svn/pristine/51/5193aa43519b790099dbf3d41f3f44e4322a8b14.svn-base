using System.Collections.Generic;
using UnityEngine;
namespace DataClass
{

    public class ConfigAttributeManager : ConfigDataManager<ConfigAttributeManager,ConfigAttribute>

    {
    
    public override void Init( )
    {
    name = "ConfigAttribute";
    ConfigAttribute config = null;
    
    config = new ConfigAttribute();
    config.id = 0;
    config.name = "力量";
    allDatas.Add( config.id, config)
;
    config = new ConfigAttribute();
    config.id = 1;
    config.name = "悟性";
    allDatas.Add( config.id, config)
;
    config = new ConfigAttribute();
    config.id = 2;
    config.name = "体质";
    allDatas.Add( config.id, config)
;
    config = new ConfigAttribute();
    config.id = 3;
    config.name = "根骨";
    allDatas.Add( config.id, config)
;
    config = new ConfigAttribute();
    config.id = 4;
    config.name = "灵巧";
    allDatas.Add( config.id, config)
;
    config = new ConfigAttribute();
    config.id = 5;
    config.name = "生命";
    allDatas.Add( config.id, config)
;
    config = new ConfigAttribute();
    config.id = 6;
    config.name = "内力";
    allDatas.Add( config.id, config)
;
    base.Init();
    }
}
}